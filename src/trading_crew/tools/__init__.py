"""CrewAI Tools — wrappers that give agents access to external services.

Each tool wraps a service (exchange, database, notifications) and exposes it
to CrewAI agents as a callable tool with a description and schema.
"""
